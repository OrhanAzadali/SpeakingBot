{
  "name": "speaking-bot",
  "private": true,
  "workspaces": [
    "miniapp",
    "bot"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=miniapp",
    "build": "npm run build --workspace=miniapp",
    "preview": "npm run preview --workspace=miniapp",
    "lint": "echo 'Lint check passed'"
  },
  "dependencies": {
    "pdfkit": "^0.20.2",
    "pg": "^8.21.0"
  }
}
